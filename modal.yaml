# modal.yaml
name: ragen-webshop-trainer
description: "RAGEN + A*PO + Qwen WebShop Training System"

image:
  base: debian-slim
  python_version: "3.10"

secrets:
  - name: my-huggingface-secret

functions:
  train:
    gpu: A10G
    timeout: 86400
    secrets:
      - my-huggingface-secret